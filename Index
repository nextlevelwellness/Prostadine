<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Prostadine – Official Website</title>
  <meta name="description" content="Prostadine official information page. Access the official purchase page securely.">
  <meta name="robots" content="index,follow">

  <style>
    :root{
      /* Prostadine-like navy/blue palette */
      --navy:#0b1f3a;
      --blue:#1f5aa6;
      --blue2:#2a6cc3;
      --bg:#f6f8fb;
      --card:#ffffff;
      --text:#0f172a;
      --muted:#475569;
      --border:#e6edf6;
      --shadow: 0 14px 32px rgba(2, 6, 23, 0.10);
    }

    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: Arial, Helvetica, sans-serif;
      background:
        radial-gradient(1200px 520px at 18% -10%, rgba(31,90,166,.16), transparent 60%),
        radial-gradient(900px 480px at 100% 0%, rgba(11,31,58,.14), transparent 60%),
        var(--bg);
      color:var(--text);
      line-height:1.6;
    }

    .container{max-width:1100px; margin:0 auto; padding:20px;}

    /* Header */
    header{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:14px;
      flex-wrap:wrap;
      background:var(--navy);
      border:1px solid rgba(255,255,255,.10);
      border-radius:16px;
      padding:14px 18px;
      box-shadow: var(--shadow);
    }
    header img{
      height:40px;
      width:auto;
      display:block;
    }
    .header-pill{
      color:#eaf2ff;
      font-size:13px;
      font-weight:700;
      border:1px solid rgba(255,255,255,.18);
      background:rgba(255,255,255,.06);
      padding:8px 12px;
      border-radius:999px;
      white-space:nowrap;
    }

    /* Hero */
    .hero{
      margin-top:18px;
      background:var(--card);
      border:1px solid var(--border);
      border-radius:22px;
      padding:26px;
      box-shadow: var(--shadow);
      display:grid;
      grid-template-columns: 1.15fr 0.85fr;
      gap:22px;
      align-items:center;
    }
    @media(max-width:920px){ .hero{grid-template-columns:1fr;} }

    h1{
      margin:0 0 10px;
      font-size:38px;
      line-height:1.12;
      letter-spacing:-0.4px;
      color:var(--navy);
    }
    .lead{
      margin:0 0 12px;
      color:var(--muted);
      font-size:16px;
    }
    .fine{
      margin:0;
      color:#64748b;
      font-size:13px;
    }

    /* CTA */
    .cta{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      margin-top:16px;
      padding:14px 22px;
      background: linear-gradient(90deg, var(--blue), var(--blue2));
      color:#fff;
      text-decoration:none;
      font-weight:900;
      border-radius:14px;
      box-shadow: 0 16px 28px rgba(31,90,166,.20);
      border:1px solid rgba(255,255,255,.18);
    }
    .cta:hover{ transform: translateY(-1px); }
    .cta:active{ transform: translateY(0); }

    /* Media */
    .media{
      display:flex;
      flex-direction:column;
      gap:14px;
      align-items:center;
    }
    .product-main{
      width:100%;
      max-width:420px;
      height:auto;
      display:block;
      filter: drop-shadow(0 20px 24px rgba(2,6,23,.20));
    }
    .seal{
      width:100%;
      max-width:620px;
      height:auto;
      display:block;
      opacity:.98;
      filter: drop-shadow(0 14px 18px rgba(2,6,23,.12));
    }

    /* Sections */
    section{
      margin-top:18px;
      background:var(--card);
      border:1px solid var(--border);
      border-radius:18px;
      padding:22px;
      box-shadow: var(--shadow);
    }
    h2{ margin:0 0 10px; color:var(--navy); font-size:22px; letter-spacing:-0.2px; }
    ul{ margin:10px 0 0; padding-left:18px; color:var(--muted); }
    li{ margin:7px 0; }

    .split{
      display:grid;
      grid-template-columns:1fr 1fr;
      gap:18px;
      align-items:center;
      margin-top:14px;
    }
    @media(max-width:820px){ .split{grid-template-columns:1fr;} }

    .img-card{
      background: linear-gradient(180deg, rgba(31,90,166,.10), rgba(11,31,58,.06));
      border:1px solid rgba(31,90,166,.18);
      border-radius:18px;
      padding:16px;
      display:flex;
      align-items:center;
      justify-content:center;
      min-height:260px;
    }
    .img-card img{
      width:100%;
      max-width:460px;
      height:auto;
      display:block;
      filter: drop-shadow(0 18px 22px rgba(2,6,23,.14));
    }

    footer{
      margin:20px 0 10px;
      background:var(--card);
      border:1px solid var(--border);
      border-radius:16px;
      padding:18px;
      box-shadow: var(--shadow);
      color:#334155;
      font-size:13px;
      white-space:pre-line;
    }
  </style>
</head>

<body>
  <div class="container">

    <!-- HEADER -->
    <header>
      <img src="prostadine-logo.jpg" alt="Prostadine Logo">
      <div class="header-pill">Official Purchase Page Access</div>
    </header>

    <!-- HERO -->
    <div class="hero">
      <div>
        <h1>Prostadine</h1>
        <p class="lead">
          Prostadine is a liquid dietary supplement designed for a simple daily routine.
          For authenticity, support, and official policies, access the official order page through the button below.
        </p>
        <p class="fine">
          You will be redirected to the official website for current offers, shipping details, and checkout.
        </p>

        <a class="cta" href="https://myprostadine24.com/text.php#aff=LorenaRocha" target="_blank" rel="noopener noreferrer">
          Click here to buy →
        </a>
      </div>

      <div class="media">
        <img class="product-main" src="prostadine-main.jpg" alt="Prostadine main product image">
        <img class="seal" src="prostadine-seal.jpg" alt="Product certifications and seals">
      </div>
    </div>

    <!-- SECTION -->
    <section>
      <h2>Why choose the official website?</h2>
      <ul>
        <li>Helps ensure you are purchasing the authentic product.</li>
        <li>Provides the most up-to-date offers and availability.</li>
        <li>Includes official shipping and customer support information.</li>
      </ul>

      <div class="split">
        <div class="img-card">
          <img src="prostadine-secondary.jpg" alt="Prostadine secondary product image">
        </div>

        <div>
          <h2>Get your Prostadine today</h2>
          <p class="lead" style="margin:0;">
            For secure checkout and official information, proceed to the official purchase page.
          </p>

          <a class="cta" href="https://myprostadine24.com/text.php#aff=LorenaRocha" target="_blank" rel="noopener noreferrer">
            Click here to get your product →
          </a>
        </div>
      </div>
    </section>

    <!-- FOOTER (exact text requested) -->
    <footer>
Statements on this website have not been evaluated by the Food and Drug Administration. Products are not intended to diagnose, treat, cure or prevent any disease. If you are pregnant, nursing, taking medication, or have a medical condition, consult your physician before using our products.

*For international shipping (outside of the United States), shipping fees will apply. Read more here.
Copyright 2025 Prostadine | All Rights Reserved
    </footer>

  </div>
</body>
</html>
